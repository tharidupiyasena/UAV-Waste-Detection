[phases.build]
cmds = [
  "pip install -r requirements.txt",           # install everything first
  "pip uninstall -y opencv-python || true",    # remove the GUI version
  "pip install opencv-python-headless"         # force the server version
]
